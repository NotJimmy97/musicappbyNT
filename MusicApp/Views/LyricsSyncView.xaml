<UserControl x:Class="MusicApp.Views.LyricsSyncView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="850">

    <!--
        KIEN TRUC GIAO DIEN: LyricsSyncView.xaml - Giao dien loi bai hat dong bo thoi gian thuc
        
        - Tac dung: Hien thi loi bai hat dong bo theo thoi gian phat cua ca khuc (Synced LRC Lyrics)
          voi phong cach Karaoke hien dai, ho tro click vao dong loi de tua nhac tuc thi (Click-to-Seek).
          
        - Van de giai quyet:
          1. Hieu ung thi giac dong loi hat dang phat: Su dung DataTrigger tren thuoc tinh IsCurrentLine
             de phong to co chu (FontSize 18 -> 24), tang do dam (FontWeight Bold), doi mau chu sang trang/accent
             va giam do trong suot (Opacity 0.45 -> 1.0). Cac dong loi xung quanh mo di de tao chieu sau thi giac.
          2. Tua nhac truc tiep tu loi bai hat (Click-to-Seek): Nguoi dung co the bam vao bat ky dong loi nao
             de goi SeekCommand tren LyricsViewModel, dua vi tri phat nhac den ngay thoi diem bat dau cua cau hat.
          3. Xu ly trang thai khong co loi (Fallback State): Hien thi panel thong bao truc quan khi bai hat
             khong co du lieu loi dong bo hoac dang trong qua trinh tai tu server/file noi bo.
             
        - Cach thuc van hanh:
          1. View nhan DataContext la LyricsViewModel.
          2. ListBox binding vao ObservableCollection LyricsLines.
          3. Khi vi tri phat nhac thay doi, LyricsViewModel tim dong loi phu hop va cap nhat IsCurrentLine=true,
             dong thoi phat su kien ActiveLineChanged de code-behind LyricsSyncView.xaml.cs can giua dong loi.
    -->

    <Grid Background="{DynamicResource WindowBackgroundBrush}">
        <Grid.RowDefinitions>
            <!-- Row 0: Top Song Info Header -->
            <RowDefinition Height="Auto" />
            <!-- Row 1: Central Synchronized Lyrics Scroll Viewer -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Row 0: Header -->
        <Border Grid.Row="0"
                Background="{DynamicResource CardBackgroundBrush}"
                BorderBrush="{DynamicResource CardBorderBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="18,14"
                Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding CurrentSongTitle}"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="{DynamicResource TitleForegroundBrush}"
                               TextTrimming="CharacterEllipsis" />
                    <TextBlock Text="{Binding CurrentSongArtist}"
                               FontSize="13"
                               Foreground="{DynamicResource SubtitleForegroundBrush}"
                               TextTrimming="CharacterEllipsis"
                               Margin="0,3,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="{DynamicResource CardSurfaceBrush}"
                            BorderBrush="{DynamicResource CardBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="12"
                            Padding="12,5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="● " FontSize="10" Foreground="{DynamicResource AccentBrandBrush}" VerticalAlignment="Center" />
                            <TextBlock Text="LỜI BÀI HÁT ĐỒNG BỘ (LRC)"
                                       FontSize="11"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource AccentBrandBrush}"
                                       VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Row 1: Central Synchronized Lyrics Container -->
        <Border Grid.Row="1"
                Background="{DynamicResource CardBackgroundBrush}"
                BorderBrush="{DynamicResource CardBorderBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="24,16">
            <Grid>
                <!-- Empty State (No Lyrics) -->
                <StackPanel VerticalAlignment="Center"
                            HorizontalAlignment="Center">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasLyrics}" Value="False">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <TextBlock Text="♫"
                               FontSize="48"
                               Foreground="{DynamicResource SubtitleForegroundBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,12" />
                    <TextBlock Text="Chưa có lời bài hát cho bài hát này"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TitleForegroundBrush}"
                               HorizontalAlignment="Center" />
                    <TextBlock Text="Hỗ trợ nạp tự động từ tệp .lrc cùng thư mục hoặc lời bài hát tích hợp sẵn."
                               FontSize="12"
                               Foreground="{DynamicResource SubtitleForegroundBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,6,0,0" />
                </StackPanel>

                <!-- Synchronized Lyrics ScrollViewer -->
                <ScrollViewer x:Name="LyricsScrollViewer"
                              VerticalScrollBarVisibility="Hidden"
                              HorizontalScrollBarVisibility="Disabled"
                              Focusable="False">
                    <ScrollViewer.Style>
                        <Style TargetType="ScrollViewer">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasLyrics}" Value="False">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ScrollViewer.Style>

                    <ListBox x:Name="LyricsListBox"
                             ItemsSource="{Binding Lines}"
                             Background="Transparent"
                             BorderThickness="0"
                             ScrollViewer.VerticalScrollBarVisibility="Disabled"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             HorizontalContentAlignment="Center"
                             Padding="0,150,0,250">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Center" />
                                <Setter Property="Padding" Value="0,4" />
                                <Setter Property="Margin" Value="0,4" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Button Command="{Binding SeekCommand}"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        Padding="16,8">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="Transparent" CornerRadius="8">
                                                <ContentPresenter />
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>

                                    <TextBlock Text="{Binding Text}"
                                               TextAlignment="Center"
                                               TextWrapping="Wrap">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="FontSize" Value="17" />
                                                <Setter Property="FontWeight" Value="SemiBold" />
                                                <Setter Property="Foreground" Value="#707884" />
                                                <Setter Property="Opacity" Value="0.65" />
                                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
                                                <Setter Property="RenderTransform">
                                                    <Setter.Value>
                                                        <ScaleTransform ScaleX="1.0" ScaleY="1.0" />
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                        <Setter Property="FontSize" Value="25" />
                                                        <Setter Property="FontWeight" Value="Bold" />
                                                        <Setter Property="Foreground" Value="{DynamicResource AccentBrandBrush}" />
                                                        <Setter Property="Opacity" Value="1.0" />
                                                        <Setter Property="RenderTransform">
                                                            <Setter.Value>
                                                                <ScaleTransform ScaleX="1.04" ScaleY="1.04" />
                                                            </Setter.Value>
                                                        </Setter>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Button>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>

